{
  "extends": "../../tsconfig.json",
  // already specified in build scripts, needed to prevent TS6305
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    "types": ["jest", "jest-extended"]
  }
}
